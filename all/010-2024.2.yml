---
# generated-by: sync-mirror
# Self-retiring backward-compat layer for release 2024.2.
#
# Upstream group_vars/all keys that a newer supported release dropped but
# that release 2024.2 still defines. Loads between the 001-* mirror and
# 099 (OSISM opinions). Values are re-serialized, not byte-copied:
# verbatim is enforced on the 001 layer only. No OSISM edits belong here.
#
# Remove this file when 2024.2 leaves the supported release range.
# Source: openstack/kolla-ansible ansible/group_vars/all, at the pinned
# commit for each supported release in this run:
#   2024.1: 72d6b6ac6ad4dbc2564ccc4472069a65a3e50111
#   2024.2: e3f40c8cbdc0a4a59699d11cb2bcca0d797b0ac3
#   2025.1: 815530a444814235fbcc1175ff11baa90df3a49a
#   2025.2: 6112487ba772f31a16051016f1247a0ab754aefe
default_container_dimensions_el9: "{{ default_docker_dimensions_el9 if kolla_container_engine == 'docker' else default_podman_dimensions_el9 }}"
default_docker_dimensions_el9:
  ulimits:
    nofile:
      soft: 1048576
      hard: 1048576
default_podman_dimensions_el9:
  ulimits:
    RLIMIT_NOFILE:
      soft: 1048576
      hard: 1048576
    RLIMIT_NPROC:
      soft: 1048576
      hard: 1048576
enable_swift_recon: "no"
enable_swift_s3api: "no"
swift_account_server_port: "6001"
swift_container_server_port: "6002"
swift_object_server_port: "6000"
swift_proxy_server_listen_port: "{{ swift_proxy_server_port }}"
swift_rsync_port: "10873"
